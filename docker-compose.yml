services:
  flask-app:
    image: noraknorak/ai:latest
    container_name: flask_container
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=deploy
    env_file:
      - .env.deploy
    networks:
      - backend_app-network

networks:
  backend_app-network:
    external: true