services:
  fastapi-app:
    image: noraknorak/ai:latest
    container_name: fastapi-app
    ports:
      - "5000:5000"
    environment:
      - FASTAPI_ENV=local
    env_file:
      - .env.local
    networks:
      - backend_app-network

networks:
  backend_app-network:
    external: true